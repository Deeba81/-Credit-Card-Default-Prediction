# -Credit-Card-Default-Prediction
Conducted EDA on 35k+ customer records, engineered key features, and applied preprocessing (imputation, outlier handling, encoding, scaling) to improve data quality. • Built and compared ML models (Logistic Regression, KNN, SVM, Decision Tree, Random Forest), achieving ~90% accuracy with evaluation using F1-score, Confusion Matrix, and ROC curves.
